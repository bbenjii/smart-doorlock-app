import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        
    },
    
    header:{
        padding: 4,
    },
    
    header_top: {
        
}
})

export default styles;